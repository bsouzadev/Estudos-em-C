#include <stdio.h>

int main() {
    // Laço 'for' que repete de i = 1 até i <= 5
    for (int i = 1; i <= 5; i++) {
        // Imprime o valor atual de i
        printf("for: %d\n", i);
    }

    return 0;
}
